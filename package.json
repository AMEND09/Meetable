{
  "name": "meetable-extension",
  "version": "1.0.0",
  "description": "Meetable accessibility extension tests",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
